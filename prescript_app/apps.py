from django.apps import AppConfig


class PrescriptAppConfig(AppConfig): # This class defines the configuration for the "prescript_app" Django application. It inherits from AppConfig and sets the name attribute to 'prescript_app', which is used by Django to identify the application within the project.
    name = 'prescript_app'
